<%= form_for(@ruolo) do |f| %>
  <% if @ruolo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ruolo.errors.count, "error") %> prohibited this ruolo from being saved:</h2>

      <ul>
      <% @ruolo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :descrizione %><br />
    <%= f.text_area :descrizione %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
