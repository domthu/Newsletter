<%= form_for(@regione) do |f| %>
  <% if @regione.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@regione.errors.count, "error") %> prohibited this regione from being saved:</h2>

      <ul>
      <% @regione.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :idregione %><br />
    <%= f.text_field :idregione %>
  </div>
  <div class="field">
    <%= f.label :regione %><br />
    <%= f.text_field :regione %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
